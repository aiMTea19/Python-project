def EX3_1(x):
    """
    (int) -> (int)
    """
    col = 0
    while x != 1 :
        if x % 2 == 0 :
            x /= 2
            col += 1    
        else :
            x = (x * 3) + 1
            col += 1
    return col
